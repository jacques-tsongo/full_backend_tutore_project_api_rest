<nav class="topbar public-nav">
  <a class="brand" href="/">
    <img src="/icons/icone_logo_application.png" alt="LinkEmploi" class="brand-logo">
  </a>
  <div class="nav-actions">
    <a class="nav-link" href="/about">À propos</a>
    <a class="nav-link" href="/contact">Contact</a>
    <% if (user) { %>
      <a class="btn primary" href="/dashboard"><%- icon('layout-dashboard') %> Tableau de bord</a>
    <% } else { %>
      <a class="btn" href="/login"><%- icon('log-in') %> Connexion</a>
      <a class="btn primary" href="/register"><%- icon('user-plus') %> Inscription</a>
    <% } %>
    <button class="btn icon-btn" data-theme-toggle aria-label="Changer de thème" title="Changer de thème"><%- icon(theme === 'dark' ? 'sun' : 'moon') %></button>
  </div>
</nav>
